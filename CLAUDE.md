# FlowLens

我正在构建一个轻量级AI工作流分析工具，项目名叫 FlowLens，使用 React/Next.js 实现。
FlowLens 的目标是：用户输入一段日常工作描述，系统识别其中重复、耗时、规则明确的任务，并生成 AI 自动化建议、优先级评分、SOP 和可复用 Prompt。
项目需要在1天内完成。

每次调用spec-dev生成四份文件后不要直接开发，要主动询问用户是否开始。

如果你在开发中进行了关键设计（尤其是实现的细节）请在docs下创建一个新markdown文档并写入，后续的也同样增加到这个文件里。
不要因为在plan等文档里已有就不写。
如果在开发或测试中遇到了值得记录的问题/BUG请在docs目录下另外创建一个文件并记录，写清楚问题描述和解决方案。
## 语言
中文回答，中文注释。

## 硬性要求

公网可访问：部署到 Vercel / Cloudflare Pages / Netlify 等，提交可打开的 URL。
GitHub 公开仓库：要有真实的 commit 历史（边做边提交），不要最后一次性传一个 commit。
英文 README：包含项目是什么、怎么本地跑起来、技术栈、已知限制（known limitations）。
移动端不崩：手机打开布局不乱、文字不溢出。
不要把任何 API key 提交到仓库里。


