import type { TaskAnalysis } from "@/lib/types";
import { ScoreBar } from "./ScoreBar";

type Props = { task: TaskAnalysis; copied: boolean; onCopy: () => void };
export function TaskCard({ task, copied, onCopy }: Props) {
  const dims = [["重复", task.dimensions.repetition, "bg-sky-100 text-sky-800"], ["耗时", task.dimensions.timeCost, "bg-amber-100 text-amber-800"], ["规则", task.dimensions.ruleClarity, "bg-violet-100 text-violet-800"]] as const;
  return <article className="rounded-xl border border-slate-200 bg-white p-5 shadow-sm sm:p-7"><div className="flex flex-col gap-4 sm:flex-row sm:items-start sm:justify-between"><div><p className="mb-2 text-xs font-semibold tracking-wider text-teal-700">TASK {task.id.replace("task-", "").padStart(2, "0")}</p><h2 className="break-words text-xl font-bold text-slate-900">{task.title}</h2><p className="mt-2 whitespace-pre-wrap break-words text-sm leading-6 text-slate-600">{task.rawText}</p></div><div className="w-full shrink-0 sm:w-48"><p className="mb-2 text-xs font-medium text-slate-500">优先级</p><ScoreBar score={task.priorityScore} /></div></div><div className="mt-6 flex flex-wrap gap-2">{dims.filter(([, value]) => value > 0).map(([label, value, cls]) => <span key={label} className={`rounded-full px-3 py-1 text-xs font-semibold ${cls}`}>{label} {value}</span>)}{task.matchedKeywords.map((word) => <span key={word} className="rounded-full border border-slate-200 px-3 py-1 text-xs text-slate-600">#{word}</span>)}</div><div className="mt-7 grid gap-6 lg:grid-cols-2"><section><h3 className="text-sm font-semibold text-slate-900">自动化建议</h3><ul className="mt-3 space-y-2 text-sm leading-6 text-slate-600">{task.automationSuggestions.map((item) => <li key={item} className="flex gap-2"><span className="text-teal-600">✓</span><span className="break-words">{item}</span></li>)}</ul></section><section><h3 className="text-sm font-semibold text-slate-900">标准作业流程</h3><div className="mt-3 space-y-3 text-sm leading-6 text-slate-600"><p><b className="text-slate-800">触发：</b>{task.sop.trigger}</p><p><b className="text-slate-800">输入：</b>{task.sop.inputs.join("、")}</p><div><b className="text-slate-800">步骤：</b><ol className="mt-1 list-decimal space-y-1 pl-5">{task.sop.steps.map((step) => <li key={step}>{step}</li>)}</ol></div><p><b className="text-slate-800">输出：</b>{task.sop.output}</p></div></section></div><section className="mt-7 border-t border-slate-100 pt-5"><div className="flex items-center justify-between gap-3"><h3 className="text-sm font-semibold text-slate-900">可复用 Prompt</h3><button type="button" onClick={onCopy} className="min-h-10 rounded-lg border border-slate-200 px-3 text-sm font-medium text-slate-700 hover:border-teal-400 hover:text-teal-700">{copied ? "已复制" : "复制"}</button></div><pre className="mt-3 max-h-56 overflow-auto whitespace-pre-wrap break-words rounded-lg bg-slate-50 p-4 text-xs leading-6 text-slate-600">{task.reusablePrompt}</pre></section></article>;
}
